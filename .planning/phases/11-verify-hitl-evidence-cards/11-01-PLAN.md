# Plan 11-01 — HITL-01 Verify evidence cards

## Goal

Improve **Verify** so operators see **what will change** before **Approve & Load**, with **graph-grounded evidence** per warning.

## Tasks

1. **`cleanup_review.py`**
   - `graph_entity_labels` — map node id → markdown label (name + kind).
   - `_graph_for_warning_scene`, `_relationship_rows_matching_key` — helpers.
   - `warning_hitl_approve_preview` — one-line Approve outcome.
   - `warning_hitl_evidence_markdown` — markdown for duplicate / lexicon / indexed rel / fallback detail.

2. **`app.py` (Verify tab)**
   - Group warning cards **by scene** (heading from matching entry when present).
   - **`st.warning`** banner for `completeness` and `audit_skipped` (no automatic graph edit).
   - **`st.info`** Approve preview above existing guidance.
   - Expander **Evidence from extracted graph** → `st.markdown(evidence)`.

## Verification

- Smoke: duplicate_relationship warning with two USES rows → preview mentions merge count; evidence lists both quotes.
- No regression: `cleanup_warning_widget_id` still uses global warning index `wi` for decisions + load.

## Out of scope (Phase 12+)

- Bulk approve, filters, CSV export.
