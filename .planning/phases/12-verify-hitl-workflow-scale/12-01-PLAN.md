# Plan 12-01 — HITL-02 Verify workflow scale

## Goal

Scale HITL when many warnings exist: **filter** by check type, **sort** scenes and cards, **bulk Approve** for `duplicate_relationship` with confirmation.

## Tasks

1. **`app.py` Verify**
   - Multiselect filter by `check` (empty = all types).
   - Select **Order scenes**: ascending/descending scene number, fewest/most warnings first.
   - Select **Sort within each scene**: pipeline order, check A–Z, severity (errors first).
   - Expander **Bulk approve all** visible `duplicate_relationship` warnings — checkbox confirm + button.
   - Per-scene expander **Bulk approve duplicates in this scene** (≥2 duplicate warnings) — checkbox + button.

2. **Docs**
   - Update `12-CONTEXT.md` status; add this plan + summary; ROADMAP / STATE / PROJECT.

## Verification

- With mixed warnings, filter reduces visible cards; widget ids unchanged (global indices).
- Bulk approve sets `cleanup_warning_widget_id` entries; **Approve & Load** still applies edits in warning order.

## Notes

- Applying multiple approvals for the same duplicate tuple at load runs collapse multiple times; second pass is a no-op when one row remains.
