---
phase: 02-graph-reliability-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - agent.py
autonomous: true
requirements:
  - REL-01

must_haves:
  truths:
    - Dashboard `@st.cache_data` Neo4j loaders return empty sentinel values on driver/session/query failures and log with logging.exception (no st.* inside cache)
    - Payoff matrix, momentum chart, and power-shift renderers guard required columns/keys before DataFrame indexing or Plotly
    - Importing `agent` does not require Neo4j to be reachable; Investigate tab surfaces connection/query failures as operator messages, not full-page tracebacks
    - Cleanup Review correction rows use safe dict access so malformed pipeline_results do not raise KeyError
  artifacts:
    - app.py updated cached loaders and render guards
    - agent.py uses lazy graph/chain initialization
  key_links:
    - tab_efficiency Neo4j read pattern (try/except + st.error + empty rows) remains the reference for non-cached reads
---

<objective>
Implement **REL-01** and **D-01–D-08** from `02-CONTEXT.md`: harden all dashboard Neo4j `@st.cache_data` loaders, guard chart DataFrame paths, lazy-init the LangChain Neo4j graph for Investigate, wrap chat invoke errors, and harden Cleanup Review iteration over corrections.

Output: `app.py` (cached loaders, `_render_*`, dashboard id tuple, Cleanup loop, Investigate tab), `agent.py` (lazy `_get_chain` / failure caching).
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-graph-reliability-empty-states/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@strategy.md
@app.py
@agent.py
@metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logging + Neo4j exception imports in app.py</name>
  <files>app.py</files>
  <read_first>app.py (lines 1–35, Neo4j cached section ~134–227)</read_first>
  <action>Add near other stdlib/third-party imports: `import logging` and `from neo4j.exceptions import AuthError, Neo4jError, ServiceUnavailable`. Set module logger: `_log = logging.getLogger(__name__)` (name may be `__name__` only; match existing style if a logger already exists).</action>
  <acceptance_criteria>
    - `rg "import logging" app.py` matches at least one line
    - `rg "neo4j.exceptions" app.py` matches `AuthError`, `Neo4jError`, `ServiceUnavailable`
    - `rg "getLogger" app.py` matches a line defining the app module logger
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wrap all seven `_cached_*` Neo4j loaders</name>
  <files>app.py</files>
  <read_first>app.py (`_cached_momentum_rows` through `_cached_event_count`, lines ~138–226)</read_first>
  <action>Per **D-01**, for each of: `_cached_momentum_rows`, `_cached_payoff_props`, `_cached_top_characters`, `_cached_primary_lead`, `_cached_act_bounds`, `_cached_act_passivity_matrix`, `_cached_event_count`:

1. Keep existing `get_driver()` + `try`/`finally` driver close when the function owns `drv`.
2. Wrap the **inner** work (everything after `drv = get_driver()` through the return) so that on `Neo4jError`, `ServiceUnavailable`, `AuthError`, or `OSError`, and as a final broad `except Exception`, you **`_log.exception(...)`** (or equivalent) and return the **empty sentinel** below. Do **not** call `st.*` inside these functions.

**Exact return values on failure:**
- `_cached_momentum_rows` → `[]`
- `_cached_payoff_props` → `[]`
- `_cached_top_characters` → `[]`
- `_cached_primary_lead` → `(None, False)` — second element `False` means “not an override-driven failure flag”; on success keep current `(pid, override)` logic unchanged
- `_cached_act_bounds` → `None`
- `_cached_act_passivity_matrix` → `{}`
- `_cached_event_count` → `0`

3. For `_cached_event_count`, guard `rec["c"]` if `rec` is not None but missing key — treat as `0` (still inside try; failure of session still returns `0`).

Do **not** change TTL or cache keys.</action>
  <acceptance_criteria>
    - Each of the seven function names appears in `app.py` with a `except` block (or nested try) that returns the sentinel above
    - `rg "_log.exception" app.py` or `rg "logging.exception" app.py` shows at least one use in the cached-loader section
    - `uv run python -c "import app"` is **not** required to succeed without Streamlit context; instead run `uv run python -c "import ast; ast.parse(open('app.py').read())"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Safe `top_chars` id tuple + power-shift rows</name>
  <files>app.py</files>
  <read_first>app.py (dashboard data loads ~462–477, `_render_power_shift` ~361–417)</read_first>
  <action>After cached loads, replace building `_ids_tuple` so it **never** assumes every dict has `"id"`: use only items where `isinstance(c, dict) and c.get("id") is not None`, e.g. `tuple(str(c["id"]) for c in top_chars if isinstance(c, dict) and c.get("id") is not None)`.

In `_render_power_shift`, after confirming `top_chars` is non-empty, **filter** to `valid_chars = [c for c in top_chars if isinstance(c, dict) and c.get("id") is not None]`. If `valid_chars` is empty after filter, call `st.warning` with a single line like *Character rank data is missing ids — cannot chart power shift.* and **return**. Otherwise iterate `valid_chars` instead of `top_chars` for the Plotly loop (use `str(c["id"])` only on filtered rows).</action>
  <acceptance_criteria>
    - `rg "_ids_tuple" app.py` shows a comprehension or loop that includes `isinstance(c, dict)` and `c.get("id")`
    - `rg "valid_chars" app.py` matches OR an equivalent filter variable documents the same guard in comments (prefer explicit `valid_chars` per plan)
    - `_render_power_shift` contains `st.warning` text mentioning **id** or **missing**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: `_render_momentum_chart` and `_render_payoff_matrix` column guards</name>
  <files>app.py</files>
  <read_first>app.py (`_render_momentum_chart` ~233–317, `_render_payoff_matrix` ~320–358)</read_first>
  <action>Per **D-03**:

**Momentum:** After `df = pd.DataFrame(rows)` and existing `scene_number` check, require column **`heat`** present and non-empty for plotting. If missing or empty, `st.warning` (e.g. *Momentum data missing heat values.*) and **return** before `df["heat_num"]`.

**Payoff:** After `df = pd.DataFrame(props)`, before any `df["last_scene"]` / `df["first_scene"]` / `df["gap"]` / lambda using `r['id']`, verify columns exist: required set **`{"id", "first_scene", "last_scene", "gap"}`**. If any missing, `st.warning` (*Payoff data missing expected columns.*) and **return**. Optional: use `.get` in the `label` lambda only after this guard passes.</action>
  <acceptance_criteria>
    - `rg "heat" app.py` inside `_render_momentum_chart` shows a column check after DataFrame creation (e.g. `"heat" not in df.columns` or `in df.columns`)
    - `_render_payoff_matrix` contains a check for `first_scene`, `last_scene`, and `gap` (and `id`) before bar chart code
    - Both functions use `st.warning` on the degraded path
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Lazy Neo4j graph + chain in agent.py</name>
  <files>agent.py</files>
  <read_first>agent.py (full file, ~1–101)</read_first>
  <action>Per **D-06**, remove **module-level** instantiation of `graph = Neo4jGraph(...)` and `chain = GraphCypherQAChain.from_llm(...)` that runs at import.

Replace with:
- Module-level `Optional` holders: `_graph: Neo4jGraph | None = None`, `_chain: GraphCypherQAChain | None = None`, `_chain_init_failed: bool = False` (or equivalent).
- `def _get_chain() -> GraphCypherQAChain | None:` that: if `_chain` is not None return it; if `_chain_init_failed` return None; try to read `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD` with `os.environ.get`; if any missing, set `_chain_init_failed = True`, `logging.getLogger(__name__).warning(...)`, return None; construct `Neo4jGraph(url=..., username=..., password=..., refresh_schema=False)` and `GraphCypherQAChain.from_llm(...)` as today; assign `_graph` and `_chain`; return `_chain`. On any `Exception`, `logging.exception(...)`, set `_chain_init_failed = True`, return None.

- `ask_narrative_mri(query: str) -> str`: call `c = _get_chain()`; if `c is None`, return a **fixed user string** (no traceback): *The investigation assistant could not connect to Neo4j. Check NEO4J_URI, NEO4J_USER, NEO4J_PASSWORD in .env and that the database is reachable.*

Keep `load_dotenv()` at top. Do not remove `cypher_prompt` / `qa_prompt` / `llm` module-level unless necessary (llm can stay module-level).</action>
  <acceptance_criteria>
    - `rg "^graph = Neo4jGraph" agent.py` returns **no** match (no module-level graph assignment)
    - `rg "def _get_chain" agent.py` matches
    - `rg "ask_narrative_mri" agent.py` shows early return when chain is None
    - `uv run python -c "import agent"` exits **0** when `.env` is missing Neo4j vars (no KeyError at import)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Investigate tab — wrap `ask_narrative_mri` invoke</name>
  <files>app.py</files>
  <read_first>app.py (`tab_investigate` block ~1056–1079)</read_first>
  <action>Wrap the call `response = ask_narrative_mri(user_input)` in `try`/`except Exception as exc`: on failure, `logging.exception` and set `response` to a short operator message (e.g. *Something went wrong running the graph query. Check Neo4j and try again.*) — **do not** pass `str(exc)` to the user if it may contain connection strings; optional technical detail only inside `st.expander("Technical detail")` with `exc` or type name. Still append assistant message to `st.session_state.messages` with the friendly text.</action>
  <acceptance_criteria>
    - `rg "ask_narrative_mri" app.py` shows it inside a try block in the Investigate tab
    - `except` clause exists in that same block
    - `__main__` block in `agent.py` if present must still work: if it calls `ask_narrative_mri`, it will trigger lazy init (acceptable)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Cleanup Review — safe correction dict access</name>
  <files>app.py</files>
  <read_first>app.py (Cleanup Review corrections loop ~847–893)</read_first>
  <action>Per **D-05**, in `for corr in pr["corrections"]`: coerce safe locals — `sn = corr.get("scene_number", "?")` and `heading = (corr.get("heading") or "untitled")` instead of `corr["scene_number"]` / `corr["heading"]`. If `corr` is not a dict, **continue** to next item.</action>
  <acceptance_criteria>
    - `rg 'corr\["scene_number"\]' app.py` returns **no** matches in Cleanup Review section (use `.get` instead)
    - Loop starts with `isinstance(corr, dict)` guard or equivalent before `.get`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `uv run python -c "import ast; ast.parse(open('app.py').read()); ast.parse(open('agent.py').read())"` exits 0
- Manual UAT (executor documents in SUMMARY): start app with Neo4j stopped — Dashboard shows infos/warnings, not traceback; Investigate returns friendly string; Efficiency tab still shows `st.error` pattern
</verification>

<success_criteria>
REL-01 satisfied for cached dashboard paths, chart DataFrame paths, Investigate import and chat, and Cleanup correction iteration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-reliability-empty-states/02-01-SUMMARY.md`
</output>
