---
phase: 01-config-script-generalized-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app.py
  - metrics.py
  - MEMORY.md
autonomous: true
requirements:
  - GEN-01
user_setup: []

must_haves:
  truths:
    - User-visible strings in app.py do not hardcode production character names for operator messaging (e.g. zev/alan as assumed protagonist)
    - CLI help and MEMORY mirror describe primary lead behavior generically
  artifacts:
    - MEMORY.md updated for primary-lead resolution
  key_links:
    - metrics.py --character help text is script-neutral
---

<objective>
Implement **GEN-01** after Plan 01 wiring: neutral operator-facing copy across **all Streamlit tabs** in `app.py`, plus **metrics.py** CLI help and **MEMORY.md** snapshot. Per **D-05**, `cleanup_review.py` / `agent.py` / `pipeline_runs.py` need changes **only if** grep finds user-visible script-specific names (likely none).

Purpose: Remove Cinema Four–centric operator copy; match analysis-driven leads.

Output: Edits to `app.py` captions/markdown, `metrics.py` argparse help, `MEMORY.md`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-config-script-generalized-dashboard/01-CONTEXT.md
@app.py
@metrics.py
@MEMORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit and fix app.py operator copy</name>
  <files>app.py</files>
  <read_first>app.py (full file search for zev, alan, Cinema, protagonist copy in st.caption/st.markdown/st.header)</read_first>
  <action>Search `app.py` for case-insensitive **zev**, **alan**, **cinema four** in **operator-visible** strings (Streamlit API args). Replace with neutral copy (*primary lead*, *configured lead*, *top characters by interaction*, etc.) or interpolate resolved **primary_id** / display name where Plan 01 already exposes them. Rename remaining variables like `zev_key` if Plan 01 left any—use `primary_key` / `lead_id`. Preserve metric **definitions** in comments only if they quote strategy.md (no fake names in UI).</action>
  <verify>! rg -i "zev|alan|cinema four" app.py</verify>
  <done>No production-specific character names in st.* string literals; variables renamed.</done>
</task>

<task type="auto">
  <name>Task 2: metrics.py CLI help + MEMORY.md</name>
  <files>metrics.py, MEMORY.md</files>
  <read_first>metrics.py (argparse --character help), MEMORY.md (protagonist bullet)</read_first>
  <action>Change `--character` help from example **alan** to neutral **e.g. char_id**. Update **MEMORY.md** bullet that says **`zev`** / `PROTAGONIST_ID` to describe **SCRIPTRAG_PRIMARY_LEAD_ID** override and analysis default (reference `lead_resolution` / top interaction count). Keep line count compact per repo style.</action>
  <verify>rg "e.g. alan" metrics.py; test $? -ne 0; rg "SCRIPTRAG_PRIMARY_LEAD_ID" MEMORY.md</verify>
  <done>MEMORY aligns with new behavior; CLI help script-agnostic.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm related modules (no-op if clean)</name>
  <files>cleanup_review.py, agent.py, pipeline_runs.py</files>
  <read_first>01-CONTEXT.md D-05</read_first>
  <action>Run ripgrep for **zev|alan|cinema** (case-insensitive) in `cleanup_review.py`, `agent.py`, `pipeline_runs.py`. If **no matches**, add one-line note to **01-02-SUMMARY.md** only. If matches in user-visible strings, neutralize same as app.py. Do **not** edit auditor prompts or schema samples per D-05.</action>
  <verify>rg -i "zev|alan|cinema four" cleanup_review.py agent.py pipeline_runs.py || true</verify>
  <done>Modules verified or fixed; SUMMARY records result.</done>
</task>

</tasks>

<verification>
- `rg -i "zev" app.py` limited to comments if any; zero in `st.` string args
- MEMORY.md documents new lead behavior
</verification>

<success_criteria>
GEN-01 satisfied for app + docs; related modules audited.
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-script-generalized-dashboard/01-02-SUMMARY.md`
</output>
