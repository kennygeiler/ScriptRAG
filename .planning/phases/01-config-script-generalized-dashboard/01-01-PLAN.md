---
phase: 01-config-script-generalized-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - metrics.py
  - lead_resolution.py
  - app.py
  - .env.example
autonomous: true
requirements:
  - CONFIG-01
user_setup:
  - env_vars:
      - name: SCRIPTRAG_PRIMARY_LEAD_ID
        why: "Optional override snake_case Character id for regression warning (unset = analysis)"
      - name: SCRIPTRAG_TOP_CHARACTERS
        why: "Optional int for power-shift cohort size (default 5)"

must_haves:
  truths:
    - Primary lead for regression comes from get_top_characters_by_interaction_count(1) when SCRIPTRAG_PRIMARY_LEAD_ID is unset
    - Override env pins primary when set and present in graph; missing id shows operator notice and skips regression warning
    - No remaining sole dependency on hardcoded PROTAGONIST_ID = "zev" in app.py
  artifacts:
    - lead_resolution.py exists with documented functions
    - .env.example documents SCRIPTRAG_* variables
  key_links:
    - app.py dashboard data load uses resolved primary id in _extra tuple for act matrix
---

<objective>
Implement **CONFIG-01** and **D-10** from `01-CONTEXT.md`: analysis-derived primary lead via `get_top_characters_by_interaction_count`, optional `SCRIPTRAG_PRIMARY_LEAD_ID` override, deterministic ranking tie-break, and dashboard wiring (regression warning + top-K cohort size).

Purpose: Remove hardcoded protagonist; align with `strategy.md` Power-shift cohort metric definition.

Output: `lead_resolution.py`, `metrics.py` ORDER BY fix, `app.py` wiring, `.env.example` lines.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-config-script-generalized-dashboard/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@strategy.md
@app.py
@metrics.py
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic tie-break in top-character query</name>
  <files>metrics.py</files>
  <read_first>metrics.py (get_top_characters_by_interaction_count Cypher, lines ~772–806)</read_first>
  <action>Per **D-10**, add secondary sort: change `RETURN`/`ORDER BY` so characters with equal `tot` sort deterministically. Use `ORDER BY tot DESC, id ASC` (return alias `id` from `c.id`). Do not change edge-count semantics (CONFLICTS_WITH + USES + INTERACTS_WITH both directions).</action>
  <verify>rg "ORDER BY tot DESC" metrics.py && rg "id ASC" metrics.py</verify>
  <done>Cypher orders by tot descending then id ascending; existing callers unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: lead_resolution module + env knobs</name>
  <files>lead_resolution.py, .env.example</files>
  <read_first>metrics.py (get_top_characters_by_interaction_count signature), .env.example</read_first>
  <action>Create **lead_resolution.py** at repo root:
- `def top_characters_k() -> int` — read `SCRIPTRAG_TOP_CHARACTERS`; if unset or invalid, return **5** (same as current `TOP_INTERACTION_CHARACTERS`).
- `def resolve_primary_character_id(*, driver: Driver | None = None) -> str | None` — if `SCRIPTRAG_PRIMARY_LEAD_ID` set and non-empty, **return it stripped** (validation against graph is Task 3 in app). If unset, call `get_top_characters_by_interaction_count(1, driver=driver)`; return first row's `id` or **None** if empty list.

Add to **.env.example** (comment lines only):
```
# Optional: pin primary lead for regression warning (snake_case Character id). Unset = rank #1 by interaction count.
# SCRIPTRAG_PRIMARY_LEAD_ID=

# Optional: power-shift / cohort size (integer). Default 5.
# SCRIPTRAG_TOP_CHARACTERS=
```
No secrets in file.
</action>
  <verify>test -f lead_resolution.py && rg "SCRIPTRAG_PRIMARY_LEAD_ID" .env.example && rg "get_top_characters_by_interaction_count" lead_resolution.py</verify>
  <done>Module importable; `uv run python -c "import lead_resolution"` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Wire app.py — remove PROTAGONIST_ID, regression + cohort</name>
  <files>app.py</files>
  <read_first>app.py (lines 38–45, 400–445, 1000–1020, cached loaders for top_chars and act matrix)</read_first>
  <action>Per **D-03, D-07, D-10**:
1. **Remove** module-level `PROTAGONIST_ID = "zev"`.
2. Import `lead_resolution` helpers; use `top_characters_k()` instead of literal **5** where `TOP_INTERACTION_CHARACTERS` is used for `get_top_characters_by_interaction_count` / dashboard (keep constant name only if still needed as alias—prefer direct call to `top_characters_k()`).
3. After Neo4j-backed dashboard loads are available, compute `primary_id = resolve_primary_character_id(driver=...)` once per run (same driver pattern as existing metrics calls—use `get_driver()` from metrics or existing app pattern).
4. Build `_extra` / act matrix extra ids: **union** of top-K character ids from existing query **with** `primary_id` if not None (so primary always in matrix when present in graph).
5. **`_protagonist_regression_warning`**: rename local `zev_key` to `primary_key` (or similar). Resolve row for primary: if **override** set but character **not** in matrix keys → **`st.sidebar.info`** or **`st.info`** with message like *Primary lead id `x` not found in loaded graph — regression check skipped* and **return** without warning. If **no** primary from analysis (None) and no override → optional short sidebar note *No primary lead resolved (empty or no edges)* per D-07. If valid row, keep existing Act3 &gt; Act1 warning text per **D-09** (generic “primary lead” wording, display resolved label/id).
6. Document in sidebar expander or caption: *Primary lead: {id} (override)* vs *(analysis: top interaction count)*.

Do **not** change Phase 2 empty-state behavior beyond messages above.</action>
  <verify>! rg "^PROTAGONIST_ID = " app.py && rg "resolve_primary_character_id" app.py && rg "lead_resolution" app.py</verify>
  <done>grep finds no `PROTAGONIST_ID`; regression path uses resolver; override missing-from-graph shows notice.</done>
</task>

</tasks>

<verification>
- `uv run python -c "from lead_resolution import resolve_primary_character_id, top_characters_k"` succeeds
- `rg PROTAGONIST_ID app.py` returns no assignment to literal protagonist constant (may appear only in comments if any)
</verification>

<success_criteria>
CONFIG-01 satisfied: analysis-primary with env override; constants file updated; operator-visible resolution path documented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-script-generalized-dashboard/01-01-SUMMARY.md`
</output>
