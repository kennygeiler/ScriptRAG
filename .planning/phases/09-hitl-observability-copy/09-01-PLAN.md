# Plan 09-01 — HITL + observability copy (FLOW-01)

## Requirement

**FLOW-01:** Operator-facing copy ties **Cleanup Review** to the **HITL commit gate** after automated self-healing, and **Pipeline Efficiency** to **agentic observability** (telemetry rows).

## Scope

- **Cleanup Review** (`app.py`): `st.info` banner — self-heal already ran in Pipeline; human approves warnings; Approve & load → Neo4j; pointer to Reconcile + Data out.
- **Pipeline Efficiency** (`app.py`): caption reframed as **:PipelineRun** = production-style extractor metrics.

## Out of scope

- Multi-step progress widget / stepper UI (optional later).
- New metrics or schema changes.

## Verification

1. Copy renders without Markdown breakage in Streamlit.
2. No new Neo4j or cache behavior.

## Files

- `app.py`
