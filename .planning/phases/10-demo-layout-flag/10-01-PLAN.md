# Plan 10-01 — Demo layout env flag (DEMO-01)

## Requirement

**DEMO-01:** Optional **`SCRIPTRAG_DEMO_LAYOUT=1`** reorders Streamlit tabs for linear **agentic pipeline** storytelling: **Cleanup → Data out → Reconcile** before efficiency/dashboard; sidebar + **Investigate** copy nudge toward structured exports first.

## Scope

- `app.py`: branch tab label order and `tab_*` assignment; sidebar caption when flag set; Investigate caption extension.
- `.env.example`, `README.md` env snippet.

## Verification

1. Default (flag unset): tab order unchanged from pre–Phase 10 shipping order (Cleanup, Reconcile, Data out, …).
2. With `SCRIPTRAG_DEMO_LAYOUT=1`: **Data out** immediately follows **Cleanup Review**.
3. No duplicate tab handles; all `with tab_*` blocks still align.

## Files

- `app.py`, `.env.example`, `README.md`
